<div class="main">
  <div class="form-container">
    <h1 class="ms-2 me-2">Editar tu cuenta</h1>
    <div class="form">
      <div class="form-body">
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: 'needs-validation' }) do |f| %>
          <%= render "users/shared/error_messages", resource: resource %>
          <div class="mb-3">
            <%= f.label :name, 'Nombre', class: 'form-label' %>
            <!-- autofocus: true: このオプションは、ページが読み込まれたときに自動的にこの入力フィールドにフォーカスを設定します。これにより、ユーザーがページを開いた直後にすぐに入力を開始できます。 -->
            <!-- autocomplete 属性は、ユーザーが以前に入力した値をブラウザが記憶し、入力中に予測候補を表示するかどうかを制御します。ここでは "name" を指定しており、通常、ユーザーの名前を入力するフィールドに適した自動補完候補をブラウザに提供するよう指示しています。 -->
            <%= f.text_field :name, autofocus: true, autocomplete: "name", class: 'form-control', maxlength: 50, required: true %>
          </div>
          <div class="mb-3">
            <%= f.label :image, 'Imagen', class: 'form-label' %>
            <%= f.file_field :image, autofocus: true, autocomplete: "image", class: 'form-control' %>
          </div>
          <div class="mb-3">
            <%= f.label :email, 'Correo electrónico', class: 'form-label' %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control', required: true %>
          </div>
          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <div class="alert alert-info" role="alert">
              Currently waiting confirmation for: <%= resource.unconfirmed_email %>
            </div>
          <% end %>
          <div class="mb-3">
            <%= f.label :phone_number, 'Número de teléfono', class: 'form-label' %>
            <%= f.text_field :phone_number, autofocus: true, autocomplete: "tel", class: 'form-control', maxlength: 14, required: true %>
          </div>
          <div class="mb-3">
            <%= f.label :password, 'Contraseña', class: 'form-label' %>
            <span class="form-text text-muted">(Deja en blanco si no deseas cambiarlo)</span>
            <%= f.password_field :password, autocomplete: "new-password", class: 'form-control' %>
            <% if @minimum_password_length %>
              <div class="form-text text-muted"><%= @minimum_password_length %> caracteres mínimos</div>
            <% end %>
          </div>
          <div class="mb-3">
            <%= f.label :password_confirmation, 'Confirmación de contraseña', class: 'form-label' %>
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'form-control' %>
          </div>
          <div class="mb-3">
            <%= f.label :current_password, 'Contraseña actual', class: 'form-label' %>
            <span class="form-text text-muted">(Necesitamos tu contraseña actual para confirmar tus cambios)</span>
            <%= f.password_field :current_password, autocomplete: "current-password", class: 'form-control', required: true %>
          </div>
          <div class="d-grid gap-2 col-6">
            <%= f.submit "Actualizar", class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
    </div>
    <%= button_to "Cancelar tu cuenta", destroy_user_path(resource), method: :delete, id: "frmDestroy", class: "btn btn-danger mt-3 ms-3 mb-5" %>
  </div>
</div>
