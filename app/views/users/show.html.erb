<div class="main user-show">
  <div class="container">
    <div class="user">
      <img src="<%= "/user_images/#{@user.profile_image}" %>">
      <h2><%= @user.name %></h2>
      <p>ID de Usuario: <%= @user.id %></p>
      <% if @user.id == @current_user.id %>
        <%= link_to("Editar", "/users/#{@user.id}/edit") %>
      <% end %>
    </div>

    <ul class="user-tabs">
      <li class="active"><%= link_to("Servicios", "/users/#{@user.id}") %></li>
      <li><%= link_to("Likes!", "/users/#{@user.id}/likes") %></li>
    </ul>

    <% @user.services.order(created_at: :desc).each do |service| %>
      <%= link_to "/services/#{service.id}" do %>

        <div class="posts-index-item">
          <div class="post-left">
            <img src="<%= "/service_images/#{service.image}" %>">
          </div>
          <div class="post-right">
            <h4><%= service.title %></h4>
            <div>
              <div class="post-user-name">
                <p>El vendedor: <%= service.user.name %></p>
              </div>
              <div style="display: flex; justify-content: space-between;">
                <p><%= JSON.parse(service.delivery_method).join(", ") %></p>
                <p><%= service.service_time %> minuto(s)</p>
                <p>$<%= service.price %> MXN</p>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
