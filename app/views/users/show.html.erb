<div class="main user-show">
  <div class="container">
    <div class="user">
      <img src="<%= "/user_images/#{@user.image_name}" %>">
      <h2><%= @user.name %></h2>
      <% if @user.id == @current_user.id %>
        <%= link_to("Editar", "/users/#{@user.id}/edit") %>
      <% end %>
    </div>

    <ul class="user-tabs">
      <li class="active"><%= link_to("Servicios", "/users/#{@user.id}") %></li>
      <li><%= link_to("Likes!", "/users/#{@user.id}/likes") %></li>
    </ul>

    <% @user.services.each do |service| %>
      <div class="posts-index-item">
        <div class="post-left">
          <img src="<%= "/user_images/#{service.user.image_name}" %>">
        </div>
        <div class="post-right">
          <div class="post-user-name">
            <%= link_to(service.user.name, "/users/#{service.user.id}") %>
          </div>
          <%= link_to(service.title, "/services/#{service.id}") %>
        </div>
      </div>
    <% end %>
  </div>
</div>
