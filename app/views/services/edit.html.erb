<!-- 
renderメソッドを使用して、_form.html.erbという名前の部分テンプレート（パーシャル）をレンダリングするためのものです。
このパーシャルには、serviceオブジェクトとtext_btnというテキストを含むボタンラベルを引数として渡しています。

render 'form': これは、formという名前のパーシャルビューを呼び出します。
通常、このパーシャルは同じフォルダ内か、Railsのビューのpartialsフォルダ内に_form.html.erbとして存在します。
アンダースコア(_)は、ファイルがパーシャルであることを示しますが、renderメソッドを使用する際にはそのアンダースコアを省略します。

{ service: @service, text_btn: 'Actualizar' }: これは、パーシャルに渡すローカル変数のハッシュです。

service: @serviceは、このフォームが編集する特定のserviceオブジェクトをパーシャルに渡します。この変数を使って、フォームフィールドの初期値を設定したり、特定のサービスの属性を表示したりします。
text_btn: 'Actualizar'は、フォームの送信ボタンに「Actualizar」（更新）と表示するためのテキストを指定しています。これにより、同じフォームパーシャルを新規作成と更新の両方のコンテキストで再利用できるようになります。 
-->


<%= render 'form', { service: @service, text_btn: 'Actualizar' } %>
