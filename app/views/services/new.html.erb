<!-- 
renderメソッドを用いて、_form.html.erbという名前の部分テンプレート（パーシャル）をレンダリングしています。
この場合、serviceオブジェクトとボタンのラベルtext_btnを'Publicar'（公開する）としてパーシャルに渡しています。 

render 'form': これはformという名前のパーシャルビューを呼び出します。
このパーシャルは通常、同じフォルダ内か、Railsのビューのpartialsフォルダ内に_form.html.erbとして存在します。
アンダースコア(_)はファイルがパーシャルであることを示しますが、renderメソッドを使用する際にはそのアンダースコアを省略します。

{ service: @service, text_btn: 'Publicar' }: これはパーシャルに渡すローカル変数のハッシュです。

service: @serviceは、このフォームが操作する特定のserviceオブジェクトをパーシャルに渡します。
この変数を使って、フォームフィールドの初期値を設定したり、特定のサービスの属性を表示したりします。

text_btn: 'Publicar'は、フォームの送信ボタンに「Publicar」（公開する）と表示するためのテキストを指定しています。
これにより、同じフォームパーシャルを新規作成と更新の両方のコンテキストで再利用できるようになります。
-->

<%= render 'form', { service: @service, text_btn: 'Publicar' } %>
