#このファイルは、RailsプロジェクトでWebpackerの開発サーバーを起動するためのスクリプトです。
#Webpackerは、RailsアプリケーションでWebpackを使いやすくするためのラッパーです。
#Webpackは、JavaScriptやCSSなどのフロントエンド資源をバンドルするためのモジュールバンドラーです。
#このスクリプトは、開発環境でWebpackerを介してWebpackの開発サーバーを実行し、ホットモジュールリプレースメント（HMR）などの機能を利用する際に使われます。

#!/usr/bin/env ruby

ENV["RAILS_ENV"] ||= ENV["RACK_ENV"] || "development"
ENV["NODE_ENV"]  ||= "development"

require "pathname"
ENV["BUNDLE_GEMFILE"] ||= File.expand_path("../../Gemfile",
  Pathname.new(__FILE__).realpath)

require "bundler/setup"

require "webpacker"
require "webpacker/dev_server_runner"

APP_ROOT = File.expand_path("..", __dir__)
Dir.chdir(APP_ROOT) do
  Webpacker::DevServerRunner.run(ARGV)
end
